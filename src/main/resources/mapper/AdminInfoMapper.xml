<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wit.baojims.mapper.AdminInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wit.baojims.entity.AdminInfo">
        <id column="admin_id" property="adminId" />
        <result column="name" property="name" />
        <result column="password" property="password" />
        <result column="sa_token" property="saToken" />
        <result column="per_name" property="perName" />
    </resultMap>

    <select id="login" parameterType="HashMap" resultType="Integer">
        select admin_id from admin_info where name=#{name} AND password=#{password}
    </select>

</mapper>
